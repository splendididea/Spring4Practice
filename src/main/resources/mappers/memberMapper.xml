<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mabtis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mufasa.persistence.MemberDAO">
    <select id="getTime" resultType="string">
        select now()
    </select>
    <insert id="insertMember">
        insert into tbl_member(userid, userpw, username, email)
        VALUES (#{userid} ,#{userpw},#{username} ,#{email})
    </insert>

    <select id="readMember" resultType="com.mufasa.domain.MemberVO">
        select *
          from tbl_member
        where userid = #{userid}
    </select>

    <select id="readMemberWithPw" resultType="com.mufasa.domain.MemberVO">
        select *
          from tbl_member
        where userid = #{userid}
          and userpw = #{userpw}
    </select>
</mapper>
